//#region Import
import api from "@/core/lib/redux-toolkit/api"
//#endregion

// eslint-disable-next-line
const smsSendersManagementApis = api.injectEndpoints({
	// endpoints: (builder) => ({}),
	endpoints: () => ({}),
})

// export const {} = smsSendersManagementApis
